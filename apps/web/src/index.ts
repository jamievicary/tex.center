// SvelteKit entry will replace this in M1. Placeholder so the
// workspace package has at least one source file for tooling.

import { PROTOCOL_VERSION } from "@tex-center/protocol";

export const APP_NAME = `tex-center web (protocol v${PROTOCOL_VERSION})`;
