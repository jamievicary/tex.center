// adapter-static needs prerender enabled by default to emit a fully
// static site. Individual non-prerenderable routes (none yet) can opt
// out locally.
export const prerender = true;
export const ssr = false;
