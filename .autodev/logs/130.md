# Iteration 130

- **Start commit:** `eed6836`
- **Start time (UTC):** 2026-05-12T07:54:27Z

---

## Goal for this iteration

Verify iter 129's `/auth/google/callback` 500 fix landed on
production, as that iteration's "Notes for future iterations"
explicitly committed: "After the next push to main, confirm
`verifyLive.spec.ts` passes with `TEXCENTER_LIVE_TESTS=1` —
including the new callback probe." Small but real — a hotfix
isn't done until the live surface is back, and the harness's
push-to-main CD is async w.r.t. the iteration that committed it.

## Actions

- Probed `https://tex.center` directly:
  - At iteration start (CD for `eed6836` still `in_progress` per
    `gh run list`): `/auth/google/callback?error=fake` → **500**.
  - Watched the in-flight `flyctl deploy` run to completion
    (`gh run watch 25721218343`, ~80s).
  - Re-probed: `/auth/google/callback?error=fake` → **400** (the
    `error=access_denied`-style "Google declined" path). Fix is
    live.
- Ran `TEXCENTER_LIVE_TESTS=1 python3 -m unittest
  tests_gold.cases.test_playwright.TestPlaywrightLive` — **PASS**
  in 4.1s. The `verifyLive` suite (including the new callback
  probe added iter 129) exercises the real `tex.center` and is
  green.
- Updated `.autodev/PLAN.md` Current focus to mark iter 129
  verified-live, freeing M7.4.2 to be the unambiguous next goal.
- Added one bullet to `.autodev/FUTURE_IDEAS.md`: an in-image
  Dockerfile smoke step (`node -e "import('jose')"` or post-deploy
  callback probe) that would catch the missed-bundling failure
  class at the CI level rather than relying on a structural test
  of the Dockerfile string.

## Tests

- Live probes recorded above.
- `tests_normal/run_tests.sh`: see test-summary footer.
- Gold: live subset re-run by hand (the full gold suite runs in
  the footer via the harness).

## Notes for future iterations

- M7.4.2 (upstream supertex daemon serialise/restore wire) is the
  unblocked next ordinary goal. Sidecar side is wired (iter 120);
  the day `snapshot()` returns non-null, persistence becomes
  automatic.
- The FUTURE_IDEAS bullet for in-image smoke is worth picking up
  before the next auth-graph-touching iteration; with adapter-node
  externals, any new prod dep risks the same latent failure.


---

- **Finish time (UTC):** 2026-05-12T07:58:19Z
- **Wallclock:** 3m 52s
- **Agent exit status:** 0

---

## Test summary

- tests_normal: PASS
- tests_gold: PASS
