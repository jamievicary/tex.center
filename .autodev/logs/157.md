# Iteration 157

- **Start commit:** `b0078e8`
- **Start time (UTC):** 2026-05-12T12:36:27Z

---

## Goal for this iteration

PLAN's iter-157 stretch 1: run the iter-156
`scripts/probe-live-ws-payload.mjs` against the live deployment
and confirm `kind: "ok"`, `helloSeen`, `fileListSeen`. This is
the first live exercise of the framed-payload path on the new
1024MB runtime floor — until this passes, M8.pw.4 activation
(stretch 2) has no evidence the underlying machinery works in
prod. Stretch 2 (GCP-console OAuth client +
`TEST_OAUTH_BYPASS_KEY` secret push) remains operator-gated;
deferred.

## Approach

Follow the iter-153/155 recipe with the iter-156 probe
substituted for the upgrade-only probe. Per iter-150 hygiene:
`flyctl proxy` as `run_in_background:true`, explicit `kill`
before iteration end, verify port unbound.

## Actions

1. Confirmed preconditions: `flyctl` on PATH, `.tools/node/bin`
   available, all four required env values discoverable in
   `creds/` (`fly.token`, `fly-postgres.txt` superuser line,
   `session-signing-key.txt` raw key, UUID extracted from
   `live-user-id.txt` via regex).
2. `flyctl proxy 5435:5432 -a tex-center-db` in
   `run_in_background:true`. `/dev/tcp/127.0.0.1/5435` confirmed
   open after 3s. Proxy PID 29353.
3. First probe call: `kind: "unexpected-response"`, status 502,
   4345ms, no `helloSeen`. Consistent with resolver
   mid-cold-start (sidecar app had 3 machines: one `started`
   from iter-155 mapped to a different project, two `stopped`
   from older deploys; for the probe's chosen project
   `ae011b9e-e287-4d62-98ff-d0187ac44dd1` the resolver had to
   start a machine fresh).
4. Second probe call: **`kind: "ok"`**, `upgraded: true`,
   `helloSeen: true`, `fileListSeen: true`, `docUpdateSeen:
   true`, `files: ["main.tex"]`, `otherFrames: 0`,
   `decodeErrors: 0`, `closeCode: null`, `elapsedMs: 3365`.
   The 3s post-`file-list` quiet window expired without an
   early close. The probe's `view` control frame was sent
   successfully (script exits non-zero on send failure; exit 0).
5. Killed proxy PID 29353; verified
   `/dev/tcp/127.0.0.1/5435` refused after the kill, no
   `flyctl proxy` left in pgrep.

## Things not done (intentional)

- **M8.pw.4 activation (stretch 2).** Requires creating a GCP
  OAuth client in `console.cloud.google.com` (UI-only browser
  step) then running `scripts/google-refresh-token.mjs` which
  opens a browser. Not bundleable with autonomous iteration
  work; needs the maintainer. Stays as iter 158+.
- **DB inspection of project_machines.** Attempted but `pg`
  is not a root devDep and adding it for one-off inspection
  churns the lockfile for no lasting value. The probe-result
  evidence is sufficient.

## Tests

- `bash tests_normal/run_tests.sh`: PASS (115 tests, 45.1s).
- No code changed beyond PLAN.md. The lock-in for this
  capability is M8.pw.4 itself (already code-side complete from
  earlier iters; just operator-gated for activation).

## Test summary

- tests_normal: PASS (115 tests)
- tests_gold: harness will run

---

- **Finish time (UTC):** 2026-05-12T12:41:24Z
- **Wallclock:** 4m 57s
- **Agent exit status:** 0

---

## Test summary

- tests_normal: PASS
- tests_gold: PASS
