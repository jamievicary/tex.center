# Iteration 132

- **Start commit:** `c55cbe3`
- **Start time (UTC):** 2026-05-12T08:09:57Z

---

## Goal for this iteration

M8.smoke.0 — the build-time runtime-image smoke test promoted by
discussion/131. New CI gate that builds `apps/web/Dockerfile`, runs
it under placeholder env, and probes every server endpoint for the
iter-129 incident class (`ERR_MODULE_NOT_FOUND` / `Cannot find
package` from adapter-node externalising an npm dep that the
runtime stage doesn't carry).

## Actions

- `scripts/smoke-runtime-image.sh` — bash entrypoint that builds
  the image, starts a container on `:13000`, waits for `/healthz`,
  hits the eight endpoints from PLAN, fails on any
  module-not-found body or unexpected 5xx (allowlists the
  `oauthConfig.ts` "Server misconfigured" 500 since placeholder
  OAuth env is intentional).
- `.github/workflows/deploy.yml` — added `smoke` job; `deploy`
  now declares `needs: smoke`. Script is the single source of
  truth so local runs match CI (`bash scripts/smoke-runtime-image.sh`).
- `tests_normal/cases/test_deploy_workflow.py` — two new tests:
  `test_smoke_job_gates_deploy` (workflow structure: job present,
  script invoked, executable bit set, deploy needs smoke) and
  `test_smoke_script_probes_all_endpoints` (drift guard: every
  listed endpoint + the module-not-found scan strings are
  literally present).
- `.autodev/PLAN.md` — marked M8.smoke.0 done with iter pointer;
  rewrote priority block status; next ordinary iteration is now
  M8.pw.3.

## Resolution of the M8.smoke.0 open question

`DATABASE_URL` left unset rather than `RUN_MIGRATIONS_ON_BOOT=0`
+ a stubbed URL. Rationale: `runBootMigrations` short-circuits on
missing URL (`bootMigrations.ts:59`), and none of the probed GETs
touch the DB without an authenticated session (verified by
reading `routes/projects/+page.server.ts` and `readyz.ts`). One
fewer placeholder to keep aligned with the real env.

## Test summary

- tests_normal: PASS (109 tests, includes two new smoke-workflow
  assertions)
- tests_gold: not touched; harness will run it.

## Notes for future iterations

- Script intentionally allow-lists 5xx bodies that contain
  `Server misconfigured` — that's the deterministic
  `loadOAuthConfig()` failure path and not a module-load issue.
  If we later refactor that error message, update the script.
- Next: M8.pw.3 (real OAuth round-trip via service account). The
  iter-131 callback bug class would have been caught by pw.3 but
  not by smoke.0 alone — see discussion/131_answer.md tail.

---

- **Finish time (UTC):** 2026-05-12T08:14:28Z
- **Wallclock:** 4m 31s
- **Agent exit status:** 0

---

## Test summary

- tests_normal: PASS
- tests_gold: PASS
