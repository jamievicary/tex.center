# Iteration 93

- **Start commit:** `8a16c47`
- **Start time (UTC):** 2026-05-11T22:58:29Z

---


## Goal for this iteration

Per PLAN's "Current focus": retry the M7.0.2 first deploy of
`tex-center-sidecar`. The Dockerfile kpathsea fix landed iter 88
but iter 88 deferred the actual `flyctl deploy` rerun on wallclock
grounds. Discussion 89 confirmed the Fly org is now on a paid card.

## Actions

1. `flyctl status -a tex-center-sidecar` — surprise: the app is
   already deployed. Image
   `tex-center-sidecar:deployment-01KRCEJJHCD0FZWFBWNM9KM5PC`,
   machines `d895e7ea479958` (primary, fra) and `683437eb1e3378`
   (standby, fra), both stopped.
2. `flyctl logs --no-tail` reading: the original boot at
   2026-05-11 21:22 UTC was successful — image pulled in 59s,
   firecracker came up, `pnpm --filter @tex-center/sidecar start`
   ran, "Server listening at http://127.0.0.1:3001" emitted at
   21:23:57Z. Trial-stop fired at 21:28:55Z ("Trial machine
   stopping. To run for longer than 5m0s, add a credit card by
   visiting https://fly.io/trial."). So the iter-88 deploy did go
   through (the iteration just didn't wait long enough to see it),
   and the only thing that needed verifying was that the paid-card
   move actually unblocks the runtime.
3. `flyctl machine start d895e7ea479958` — started cleanly. After
   15s, machine state `started`, Fastify server listening on both
   the loopback and the 6PN address, no trial-stop signal in the
   new event-log entries.
4. `flyctl ssh console` probes:
   - `/opt/engine/bin/lualatex-incremental --version` →
     `LuaTeX, Version 1.25.9 (TeX Live 2027/dev)`.
   - `find / -name lualatex.fmt` → `/opt/engine/web2c/lualatex.fmt`
     (baked at image build by the iter-75 Dockerfile dump step).
   - `TEXMFCNF` env shows
     `/etc/texmf/web2c:/usr/share/texlive/texmf-dist/web2c`
     (the iter-88 kpathsea fix).
   - Compiled a trivial `\documentclass{article}\begin{document}
     hello\end{document}` end-to-end — got a valid 1-page
     `t.pdf`. Engine path is genuinely working.
5. Stopped the machine again to avoid idle burn until M7.0.3 wires
   a control-plane proxy that will warm it on demand.
6. `deploy/README.md` gained a "Live deploy state — sidecar
   (M7.0.2)" section recording image digest, machine IDs, canonical
   deploy command, and the verification steps above.
7. `.autodev/PLAN.md`: marked M7.0.2 First deploy `[x]` (with iter-
   93 pointer + summary), bumped M7.0.2 itself to `[x]`, and
   redirected "Current focus" to M7.0.3 control-plane WS proxy.

## Tests

- No `src/` or `tests_*/` changes; only `deploy/README.md` and
  `.autodev/PLAN.md`. Both runners expected green per iter-92's
  80/80 + gold-pass baseline. Harness will run them.

## Resolution

M7.0.2 closes — the sidecar Machine boots cleanly, the patched
lualatex engine runs end-to-end on Fly, and the trial-stop is gone
now that the org is on a paid card. Iter 88's deploy did succeed
in flight; iter 93 just had to verify and document it.

## Notes for future iterations

- M7.0.3 is the next slice: add `/ws/project/[id]` proxy in
  `apps/web` dialling `tex-center-sidecar.internal:3001`. The auth
  redirect already exempts `/ws/*` (see `routeRedirect.ts`); only
  the WS-pipe plumbing is needed.
- Sidecar machines were left stopped post-verification. First
  proxy hit will wake them on demand (Fly default behaviour for
  6PN apps with no explicit auto-stop config).

---

- **Finish time (UTC):** 2026-05-11T23:03:40Z
- **Wallclock:** 5m 11s
- **Agent exit status:** 0

---

## Test summary

- tests_normal: PASS
- tests_gold: PASS
