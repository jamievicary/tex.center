# Iteration 3

- **Start commit:** `13389b2`
- **Start time (UTC):** 2026-05-10T16:57:10Z

---

## Goal for this iteration

Close M0's last open subtask: wire `pnpm -r typecheck`
(`tsc --noEmit`) into `tests_normal/run_tests.sh`. Iter 2 left
this open because no Linux Node was installed in this WSL2
environment. Picking it now (over starting M1) because the
typecheck is a load-bearing safety net that subsequent
iterations get to lean on for free, and the install is
self-contained.

## Approach

PLAN's recommended option: fetch Node 20 LTS into a gitignored
`.tools/` and activate pnpm via corepack. No sudo, no host
mutation, idempotent across iterations.

## Actions

- `tests_normal/setup_node.sh`: idempotent provisioner. Pulls
  `node-v20.18.1-linux-x64.tar.xz` from nodejs.org into
  `.tools/`, symlinks `.tools/node` → the versioned dir, runs
  `corepack enable`. Re-runs are no-ops.
- `tests_normal/run_tests.sh`: now calls `setup_node.sh`,
  prepends `.tools/node/bin` to PATH, and runs
  `pnpm -r typecheck` after the structural checks.
- Added `typescript@5.6.3` as a workspace-root devDependency
  (`pnpm add -Dw`). Per-package `typecheck` scripts already
  existed (`tsc --noEmit -p tsconfig.json`).
- `.gitignore`: added `.tools/`.
- `pnpm-lock.yaml`: committed (typescript only, for now).
- `.autodev/PLAN.md`: M0 marked done; "Local toolchain"
  rewritten to describe the now-shipped provisioner; "Current
  focus" advances to M1.

## Tests

`bash tests_normal/run_tests.sh`:

```
structural_checks: OK (3 packages)
setup_node.sh: node v20.18.1, pnpm 9.12.0
Scope: 3 of 4 workspace projects
packages/protocol typecheck: Done
apps/sidecar typecheck: Done
apps/web typecheck: Done
tests_normal/run_tests.sh: PASS
```

Gold suite untouched (still placeholder).

## Notes for future iterations

- First run of `setup_node.sh` on a fresh checkout downloads
  ~25 MB. Subsequent runs are no-ops. The harness re-uses the
  working tree across iterations so this cost is paid once.
- M1 (SvelteKit shell) is the obvious next slice. SvelteKit's
  scaffolder will write into `apps/web/`; preserve the existing
  `package.json` `dependencies.@tex-center/protocol`
  (`workspace:*`) when merging. Add SvelteKit + adapter-node
  to that package, run `pnpm install`, and the typecheck
  already in place will catch most wiring issues.
- pnpm is currently 9.12.0 (matches root `packageManager`). If
  you bump it, also bump `packageManager` so corepack
  activates the same version.


---

- **Finish time (UTC):** 2026-05-10T17:02:19Z
- **Wallclock:** 5m 09s
- **Agent exit status:** 0

---

## Test summary

- tests_normal: PASS
- tests_gold: PASS
