# Iteration 94

- **Start commit:** `55efcc6`
- **Start time (UTC):** 2026-05-11T23:06:30Z

---

## Goal for this iteration

Start M7.0.3 (control-plane WS proxy). Adapter-node's emitted
`build/index.js` doesn't expose an HTTP Upgrade hook, so M7.0.3
naturally splits into: pure proxy logic (this iter), custom Node
entry + Dockerfile change, auth gating, deploy+verify. Slice 0
delivers a unit-tested pure proxy module that any later
production entry can `attachWsProxy(server, ...)` on top of.

## Actions

1. New module `apps/web/src/lib/server/wsProxy.ts` exporting:
   - `matchWsProjectPath(pathname)` — `/ws/project/<id>` →
     `<id>` where `<id>` is `^[A-Za-z0-9_-]+$` (same regex as
     `apps/sidecar/src/workspace.ts`). Anything else → `null`.
   - `resolveSidecarUpstream(env)` — defaults
     `tex-center-sidecar.internal:3001`; overridable via
     `SIDECAR_HOST` / `SIDECAR_PORT`; bad port string throws.
   - `renderForwardedHeaders(rawHeaders, upstream)` — preserves
     duplicates + casing (operates on Node's alternating
     `rawHeaders` array), rewrites `Host:` to the upstream
     authority, dedupes multiple Host headers, adds one if
     absent. Drops the `:80`/`:443` suffix on default-port
     upstreams.
   - `attachWsProxy(server, options)` — listens for `'upgrade'`,
     matches the path, opens a `net.connect` to the upstream,
     writes the reconstructed request line + headers, then
     `pipe`s the two sockets both ways. Bounded connect
     timeout (default 5s). Errors / closes trigger a single
     `cleanup()` that destroys both sockets. Unknown paths get
     a minimal `HTTP/1.1 404 Not Found` then socket destroy —
     adapter-node ignores upgrades, so a dangling socket would
     hang the client.
   - `WsProxyEvent` union published via an optional `onEvent`
     hook for tests/logs.
2. Test `apps/web/test/wsProxy.test.mjs`:
   - Pure-logic coverage of all three helpers (12+ assertions).
   - Real `http.Server` + stub upstream `net.Server` exchanging
     bytes: asserts upstream sees the rewritten `Host:`, the
     forwarded `Cookie: tc_session=stub`, and that an upstream
     write reaches the client.
   - Unknown path → client sees `HTTP/1.1 404`.
   - Invalid projectId (e.g. URL-encoded space) → 404.
   - Upstream connect-refused → client socket closes within
     the bounded timeout (no hang).
   - Asserts `upstream-connected` and `no-match` events fired.
3. Registered as `test_web_ws_proxy` in
   `tests_normal/cases/test_node_suites.py`.
4. `.autodev/PLAN.md`: split M7.0.3 into 0/1/2/3, marked .0
   `[x]`, redirected "Current focus" to M7.0.3.1.

## Tests

- `pnpm exec tsx apps/web/test/wsProxy.test.mjs` → `wsProxy ok`.
- `pnpm --filter @tex-center/web typecheck` clean (0 errors).
- Full normal + gold suites left to the harness.

## Notes for future iterations

- M7.0.3.1: write `apps/web/src/server.ts` (or `entry.ts`) that
  imports `./build/handler.js` (adapter-node's middleware
  export), creates an `http.Server` with that handler,
  `attachWsProxy`'s onto it, and `listen(PORT, HOST)`. Update
  the Dockerfile `CMD` from `node build/index.js` to the new
  entry. The handler lives at `apps/web/build/handler.js` after
  `pnpm --filter @tex-center/web build`.
- M7.0.3.2: lift `loadSessionSigningKey` + `resolveSessionHook`
  use into the upgrade handler — parse `Cookie:` from
  `req.headers.cookie`, short-circuit with `401 Unauthorized`
  before `net.connect` if no valid session.
- M7.0.3.3: live verification — the sidecar machine is currently
  `stopped`. The first proxied upgrade should wake it; allow a
  2–5s warm-up before asserting the handshake completes.

---

- **Finish time (UTC):** 2026-05-11T23:13:12Z
- **Wallclock:** 6m 42s
- **Agent exit status:** 0

---

## Test summary

- tests_normal: PASS
- tests_gold: PASS
