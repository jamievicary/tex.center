# Iteration 128

- **Start commit:** `1ce657d`
- **Start time (UTC):** 2026-05-12T07:41:20Z

---

## Goal for this iteration

Iter 127 landed `/readyz` on the control plane with DB probing.
Natural follow-up flagged there: extend `verifyLive.spec.ts` so the
next deploy-verification run exercises the new endpoint against
prod. Pure additive test slice — no production code changes.

M7.4.2 (upstream supertex serialise/restore wire) remains the
nominal PLAN focus but lives in the supertex repo. Iteration 128
is `% 10 == 8` (ordinary), so a small focused slice fits.

## Actions

- `tests_gold/playwright/verifyLive.spec.ts`: added a 6th probe
  asserting `GET /readyz` returns 200 with `ok: true`,
  `protocol: "tex-center-web-v1"`, `db.state: "up"`. Updated the
  file-header probe list to match.

## Tests

- `bash tests_normal/run_tests.sh` — 106 PASS.
- Gold not re-run (live spec is `live`-project-gated;
  `TEXCENTER_LIVE_TESTS=1` would be needed and live `DATABASE_URL`
  is set so the assertion should hold on the next deploy-verify run).

## Notes for future iterations

- First deploy-touching iteration with `TEXCENTER_LIVE_TESTS=1`
  will exercise this end-to-end alongside `verifyLiveWsUpgrade`.
- `/readyz` could also gain a `blobs` field once a control-plane
  blob store lands (already noted in `readyz.ts` and iter 127 log).


---

- **Finish time (UTC):** 2026-05-12T07:43:49Z
- **Wallclock:** 2m 29s
- **Agent exit status:** 0

---

## Test summary

- tests_normal: PASS
- tests_gold: PASS
