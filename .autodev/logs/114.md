# Iteration 114

- **Start commit:** `a60e573`
- **Start time (UTC):** 2026-05-12T01:58:04Z

---

## Goal for this iteration

Close M7.5.3. The iter-113 notes flagged that the
`compile-status:error` wire path is already in place
(`packages/protocol/src/index.ts:57` has
`{ state: "idle" | "running" | "error"; detail?: string }`,
`server.ts:218` propagates `result.error` into `detail`,
`wsClient.ts:121-126` decodes it into `lastError`), but no test
exercised the sidecar end-to-end for the failure branch — so the
plan bullet was still `[ ]` despite the code being done. Adding
that test and flipping the bullet is a clean small slice that
turns a known-correct claim into a regression-guarded one.

The "next ordinary" pick (M7.1.3.2.b — WS-upgrade-with-cookie
live probe + Machine cleanup) is still the right next-iteration
target; deliberately deferring by one to land a coverage gap that
would otherwise rot.

## Approach

`buildServer` already accepts an injectable `compilerFactory`.
A stub that returns `{ ok: false, error: "boom from stub" }`
exercises the failure branch without any LaTeX, fake-daemon, or
process scaffolding. Connect a WS client (using the existing
`bootClient` helper from `apps/sidecar/test/lib.mjs`), wait for a
`compile-status` control frame with `state: "error"` and the
expected `detail`, and additionally assert that no `pdf-segment`
and no `compile-status:idle` frame ever arrived (a green→red
regression where `runCompile` continues past the failure would
slip through a positive-only assertion).

## Actions

- `apps/sidecar/test/serverCompileError.test.mjs` — new test as
  described above.
- `tests_normal/cases/test_node_suites.py` — wire it into the
  Python runner (`test_sidecar_server_compile_error`).
- `.autodev/PLAN.md` — M7.5.3 flipped from `[ ]` to `[x]` with
  pointers to the protocol slot, server propagation site, client
  decoder, and the two tests that now cover the daemon→wire chain.
  "Current focus" reordered: M7.5.3 dropped, M7.5.4 promoted to
  the post-M7.1.4 slot.

## Tests

- `bash tests_normal/run_tests.sh` — 90 PASS (was 89; +1 from
  the new test).

## Notes for future iterations

- M7.1.3.2.b remains the next ordinary pick. It needs a teardown
  hook on the Playwright fixture that calls
  `MachinesClient.destroy` + deletes the `machine_assignments`
  row at probe end; the existing `flyProxy.ts` doesn't have a
  Machine-lifecycle escape hatch.
- M7.5.4 (gate `PdfStabilityWatcher` on compiler kind) is now
  the only open M7.5.x sub-bullet besides M7.5.5's "real ELF"
  end-to-end. `pdfStabilityWatcher.test.mjs` exists; the wiring
  in `server.ts` is the missing half.

---

- **Finish time (UTC):** 2026-05-12T02:02:28Z
- **Wallclock:** 4m 24s
- **Agent exit status:** 0

---

## Test summary

- tests_normal: PASS
- tests_gold: PASS
