# Iteration 76

- **Start commit:** `3966100`
- **Start time (UTC):** 2026-05-11T17:58:10Z

---

## Goal for this iteration

**Discussion mode** — answering `.autodev/discussion/76_question.md`
(production OAuth broken: secrets aren't in the deployed image,
`creds/` is gitignored + dockerignored). Full answer in
`.autodev/discussion/76_answer.md`.

Diagnosis confirmed by direct probe:
`GET https://tex.center/auth/google/start` → 500 with the exact
ENOENT body quoted in the question; `flyctl secrets list --app
tex-center` returned zero secrets.

## Actions

- `apps/web/src/lib/server/oauthConfig.ts` — env-first; file
  fallback (`creds/google-oauth.json`) only when
  `NODE_ENV !== "production"` AND the env var is unset. Production
  never opens the file, so a missing file can no longer mask a
  missing env var. Kept env-var names `GOOGLE_OAUTH_CLIENT_ID/
  CLIENT_SECRET` (existing convention; question's bare
  `GOOGLE_CLIENT_ID` rejected to avoid future-Google-API
  ambiguity; zero migration cost — no other callers).
- Audited every secret read in `apps/web/src`: `sessionConfig.ts`
  and `db.ts` were already env-only. `oauthConfig.ts` was the
  only file-bound secret.
- `flyctl secrets set --app tex-center` pushed
  `GOOGLE_OAUTH_CLIENT_ID`, `GOOGLE_OAUTH_CLIENT_SECRET`,
  `GOOGLE_OAUTH_REDIRECT_URI=https://tex.center/auth/google/callback`,
  fresh `SESSION_SIGNING_KEY` (32 random bytes b64url, 43 chars),
  `NODE_ENV=production`.
- `flyctl deploy --remote-only --app tex-center --config
  apps/web/fly.toml` — secrets alone wouldn't help (deployed
  image still had the old `readFileSync` code). New image
  `registry.fly.io/tex-center:deployment-01KRC3AKBBT8R1DYDVT2NZNR52`.
- Post-deploy probes (via `node -e 'fetch(...)'`):
  - `/healthz` → 200 `{"ok":true,"protocol":"tex-center-web-v1"}`.
  - `/auth/google/start` → 302 to
    `accounts.google.com/o/oauth2/v2/auth?...` with the expected
    `client_id`, `redirect_uri`, `code_challenge_method=S256`,
    `state`, `prompt=select_account`. Verifier correctly absent
    (it's in the signed state cookie).
- `deploy/VERIFY.md` added — three probes (healthz / root /
  oauth-start) with explicit pass conditions, the manual Google
  Console "Authorized redirect URIs" prerequisite called out, and
  a table of expected Fly secrets. "Stability is the signal"
  framing applied to deploys: a green `flyctl deploy` is not a
  working deploy until the probes pass.

## Tests

`bash tests_normal/run_tests.sh` — 72 passing, ~65s. No new unit
test on `loadOAuthConfig` env-vs-file precedence (would need a
temp-dir + env-stash harness not in `tests_normal/lib.sh`); noted
as a candidate FUTURE_IDEAS entry.

## What remains for the user

Add `https://tex.center/auth/google/callback` to the OAuth
client's "Authorized redirect URIs" in Google Cloud Console.
Probe 3 verifies the local-app side of the handshake only; it
cannot exercise Google's `redirect_uri_mismatch` check without
real user consent. Per `GOAL.md` §External services & credentials
this is one of the few items where surfacing an instruction to
the user is the correct move.

## Notes for future iterations

- M6.3.1 is now substantively complete: live OAuth start is
  functional, not just `/healthz`. The PLAN.md M6.3.1 narrative
  already lists this as the closing slice; this iteration closes
  the verification gap that iter-73 missed.
- When the sidecar tier (M7.0.2) ships, mirror this env-only
  pattern from day one.
- When `DATABASE_URL` is wired into the live control plane,
  extend `deploy/VERIFY.md` with a DB probe and include it in
  the same `flyctl secrets set` call.

---

- **Finish time (UTC):** 2026-05-11T18:07:59Z
- **Wallclock:** 9m 49s
- **Agent exit status:** 0

---

## Test summary

- tests_normal: PASS
- tests_gold: PASS
