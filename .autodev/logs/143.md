# Iteration 143

- **Start commit:** `41c572d`
- **Start time (UTC):** 2026-05-12T09:24:45Z

---

## Goal for this iteration

Pick a small, well-scoped item off `FUTURE_IDEAS.md` rather than
attempting M7.4.2, which is gated on an upstream supertex change
that hasn't landed (no serialise/restore wire in
`vendor/supertex/src` yet — `SupertexDaemonCompiler.snapshot()`
still returns `null` deliberately). Broaden the Google OAuth scope
from `openid email` to `openid email profile` so the verified ID
token includes the `name` claim and `findOrCreateUserByGoogleSub`
populates `display_name` for real (today it defaults to `''`).
Cosmetic, but unblocked and standalone.

## Actions

- `apps/web/src/lib/server/oauthStart.ts:58` — scope param now
  `openid email profile`.
- `apps/web/test/oauthStart.test.mjs:37` — pin the new value.
- `.autodev/FUTURE_IDEAS.md` — drop the now-implemented bullet.

The downstream wiring (`callback +server.ts` → `displayName: claims.name`
→ `findOrCreateUserByGoogleSub`) was already in place; today's change
is purely the upstream request the rest of the path was waiting on.

## Tests

- `bash tests_normal/run_tests.sh` — PASS (111 tests). The
  `oauthStart` Node suite picks up the new scope assertion.

## Notes for future iterations

- M7.4.2 remains the headline blocker but is upstream-gated.
  `vendor/supertex` PLAN.md "Candidate supertex (upstream) work"
  item 2 is the prerequisite; this project can land its
  `SupertexDaemonCompiler.snapshot/restore` the day that wire is
  available.

---

- **Finish time (UTC):** 2026-05-12T09:28:14Z
- **Wallclock:** 3m 29s
- **Agent exit status:** 0

---

## Test summary

- tests_normal: PASS
- tests_gold: PASS
