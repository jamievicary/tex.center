# Iteration 101

- **Start commit:** `6a99747`
- **Start time (UTC):** 2026-05-12T00:01:14Z

---

## Goal for this iteration

N=101 → plan-review iteration (§3). `.autodev/PLAN.md` had grown
to 763 lines, dominated by closed-milestone narrative. Prune to a
future-looking shape: live milestones detailed, closed slices
compressed to one-line entries with iter pointers, preserve
carry-overs, open risks, and scheduled deletions.

## Approach

Rewrite PLAN.md end-to-end. Keep structure (Overview, Milestones,
Current focus, Live caveats, Local toolchain, Open questions,
Candidate supertex work). Compress:

- M3 retired narrative + M3.6 subsumption text.
- M4.0–M4.2.2 (closed) collapsed to one bullet.
- M5 (closed) compressed but kept the key constraints
  (`clockTolerance`, allowlist, deferred bits).
- M6.0–M6.2.1 (closed) one-line.
- M7.0.0/0.1/0.2/0.3 sub-slices: kept essential operational facts
  (canonical deploy command, env var, `TEXMFCNF` workaround,
  vendor engine provenance) but dropped narrative about how the
  bug was found.
- M7.1.0 (closed) compressed to its interface.
- M7.5.0/5.1 (closed) compressed; **preserved the M7.5.0
  carry-over** (Dockerfile `RUN make` builds the wrong target,
  `SUPERTEX_BIN` points at a removed entry) since it's a live
  fix-on-M7.5.2 item.
- M8.pw.0/pw.1/pw.2 compressed; **preserved the
  `maxConnections: 16` workaround** (live gotcha) and the
  `globalSetup` env-after-webServer fix narrative (still load-
  bearing for understanding the current shape).

Preserve verbatim (or near-verbatim):
- All `[ ]` open work (M7.1.1–7.1.4, 7.2, 7.3, 7.4, 7.5.2–7.5.5,
  M4.3.1, M8.acceptance).
- Current focus (slightly updated to point at M7.1 not M7.0).
- Live caveats (added one bullet: `DATABASE_URL` not set on
  control plane, authed WS upgrade collapses to 401 — surfaced
  from M7.0.3.3's tail).
- Local toolchain (DrvFs note remains essential).
- Open questions / risks.
- Candidate supertex work.

## Actions

- Rewrote `.autodev/PLAN.md` (763 → 324 lines, 57% reduction).
- No `src/` or test changes.

## Tests

- `bash tests_normal/run_tests.sh` — 84 cases PASS (refactor of
  agent state only, no project code touched).
- Gold suite not re-run (no gold-suite changes).

## Notes for future iterations

- The next ordinary iteration is **M7.1.1 — DB schema for the
  project↔machine mapping**. Plan entry has the open scope.
- Carry-overs preserved that future iterations need to honour:
  - M7.5.2 must change `apps/sidecar/Dockerfile` `RUN make` to
    `make -C vendor/supertex all` and update `SUPERTEX_BIN`.
  - M7.1.3 closes the M7.0.3.3 tail (live authed-upgrade probe).
  - M7.4 closes the M4.3.2 tail (checkpoint persistence).
- The "Current focus" pointer historically diverges from
  reality fastest — re-check it on the next plan-review (iter 111).

---

- **Finish time (UTC):** 2026-05-12T00:04:24Z
- **Wallclock:** 3m 10s
- **Agent exit status:** 0

---

## Test summary

- tests_normal: PASS
- tests_gold: PASS
