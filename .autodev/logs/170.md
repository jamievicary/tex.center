# Iteration 170

- **Start commit:** `bf733be`
- **Start time (UTC):** 2026-05-12T14:49:28Z

---

## Goal for this iteration

Read iter 169's gold result and act on the FREEZE-lift slot. Result:
**M8.pw.4 (`verifyLiveFullPipeline.spec.ts`) is GREEN for the first
time, automatically, on the live deploy** — `edit → pdf-segment frame
→ non-blank preview canvas` passed in 14.9s. The 14-iter image-pin
drift uncovered in iter 169 was the last sidecar-side blocker, and
with the iter-169 `flyctl secrets set SIDECAR_IMAGE=…@sha256:56ee6e14…`
in place the spec ran against the real iter-167 sidecar image.

The only red in this iteration's gold pass is unrelated:
`projects.spec.ts:50` empty-state test on the live target asserts
`No projects yet.` is visible, but the shared live user
(`jamievicary@gmail.com`) has accumulated projects from manual use
and previous spec runs, so the empty-state can never be observed
without destroying real data. PLAN iter-164 entry already flagged
this as "not on critical path - deferred until M8.pw.4 is green".
That precondition now holds.

## Approach

The behaviour under test (empty-state markup) is identical on both
targets and runs green on `local` every iter. Running it on `live`
adds no coverage and is structurally impossible to keep green
against a shared real user. Skip the test on the live target with
an explicit reason, lock the skip with a normal-suite regression
test so it can't quietly drift back.

This is **not** the FREEZE-lift moment yet: criterion (b) per
`162_answer.md` — exercise edit→pdf-segment against a *reused
pre-existing project* — is not yet covered. That's the next slice,
not bundled here.

## Actions

- `tests_gold/playwright/projects.spec.ts`: `test.skip(
  testInfo.project.name === "live", …)` on the empty-state test,
  with a comment naming the reason (shared live user accumulates
  real projects).
- `tests_normal/cases/test_projects_empty_state_skips_live.py`:
  asserts the exact `test.skip(\n      testInfo.project.name ===
  "live"` line stays in the spec.

## Tests

`bash tests_normal/run_tests.sh` — PASS (127 tests, 78.9s).

## Notes for future iterations

- **M8.pw.4 is green.** First time ever automatic. The whole
  `apps/web` ws-proxy → `flyctl machines start` → cold-start TCP
  probe → sidecar `incoming request` → Yjs frames → SupertexDaemon
  compile → `pdf-segment` wire frame → PDF.js canvas paint loop is
  proven working live by the gold runner each iter. This was the
  iter-147 incident class.
- **FREEZE-lift remaining work:** PLAN's iter-166+ slot describes
  the reused-pre-existing-project variant of M8.pw.4. Concretely:
  insert a fixed-UUID seed project row owned by the live user (or
  reuse an existing well-known one), don't delete it in
  `afterEach`, and run the same edit→pdf-segment assertion. The
  fresh-vs-reused divergence is a known coverage hole per
  iter-162's user report.
- **Save-feedback affordance** (PLAN iter-166+) is no longer
  blocked on iter 164's green.


---

- **Finish time (UTC):** 2026-05-12T14:53:43Z
- **Wallclock:** 4m 15s
- **Agent exit status:** 0

---

## Test summary

- tests_normal: PASS
- tests_gold: PASS

### Gold cases

- ✓ test_authed_cookie (test_authed_cookie.TestAuthedCookie.test_authed_cookie)
- ✓ test_cleanup_project_machine (test_cleanup_project_machine.TestCleanupProjectMachine.test_cleanup_project_machine)
- ✓ test_fly_proxy (test_fly_proxy.TestFlyProxy.test_fly_proxy)
- ✓ test_local_db (test_local_db.TestLocalDb.test_local_db)
- ✓ test_mint_google_id_token (test_mint_google_id_token.TestMintGoogleIdToken.test_mint_google_id_token)
- ✓ test_mint_session (test_mint_session.TestMintSession.test_mint_session)
- ✓ test_machine_assignments (test_pglite.TestPglite.test_machine_assignments)
- ✓ test_migrations (test_pglite.TestPglite.test_migrations)
- ✓ test_projects (test_pglite.TestPglite.test_projects)
- ✓ test_users_sessions (test_pglite.TestPglite.test_users_sessions)
- ✓ test_supertex_daemon_real (test_supertex_daemon_real.TestSupertexDaemonReal.test_supertex_daemon_real)
