# Iteration 153

- **Start commit:** `8d7a5e6`
- **Start time (UTC):** 2026-05-12T11:57:27Z

---


## Goal for this iteration

PLAN iter-153 entry: live cutover. The iter-152 commit (8d7a5e6) is
currently triggering its `deploy-sidecar.yml` CD run (status:
in_progress, started 11:57:27Z) — the first ever attempt to actually
build the runtime stage with both submodule access (iter 151) and the
engine binary marked executable (iter 152). Once it lands a new image
sha I'll: pin it via SIDECAR_IMAGE secret on tex-center, destroy any
stale per-project Machines so the resolver re-creates them at the new
sha, then probe a real WS upgrade against tex.center.

## Actions


- Waited for sidecar CD run 25732884801 (iter-152 commit 8d7a5e6).
  First ever successful CD: status=success after ~5 min. New image
  `registry.fly.io/tex-center-sidecar@sha256:5513f7f38b57e3badf0429ba1f319486ad1b44f5ef9c1ea09c91da9f8d4fc0a9`,
  tagged `deployment-01KRE0SM0SS57JZX61PRQ58W29`, GH_SHA label confirms
  iter-152 source.
- `flyctl secrets set SIDECAR_IMAGE=<new-digest> -a tex-center` —
  rolling-updated the control-plane Machine.
- `flyctl machines destroy --force` on the two stale per-project
  Machines (185432ef5d9038 old-field, 0803d24b6d1e48 cold-lake) that
  had been OOM-rebooting on the old image.
- Ran `scripts/probe-live-ws.mjs` via the vendored `.tools/node` and a
  `flyctl proxy 5435:5432 -a tex-center-db` background tunnel (PID
  9308, killed at end). Two notes for future iterations:
  1. `pnpm exec tsx` requires `.tools/node/bin` on PATH; not on by
     default outside the test runner.
  2. `creds/live-user-id.txt` is a documentation file, not a raw
     UUID — extract with a UUID regex before passing as
     `TEXCENTER_LIVE_USER_ID`. Probe failure mode if you skip this is
     a postgres `22P02 invalid input syntax for type uuid` against
     `listProjectsByOwnerId`.

  First probe (12:02:18Z, 64s) hit the resolver mid-cold-start →
  Fly returned 502 with TWO `via` hops (real upstream response, not
  Fly-edge synthesis — confirming the iter-147 root-cause class is
  fixed and we're now in normal-Fly-routing territory). Second
  probe (12:03:52Z, 369ms) after the new per-project Machine
  `d8d545df11d078` reached state=`started`:

  ```
  kind: "upgrade", status: 101
  via: 1.1 fly.io, 1.1 fly.io
  ```

  Bogus-id probe still returns 403 (cookie valid, ACL enforced).
- Live cutover gate from PLAN (iter-153 step 4) is met.

## Caveats / things to watch

- Per-project Machines are still created at the Fly Machines API
  default guest size (~256MB). The old image OOM-rebooted on first
  real request at that size (anon-rss only 52MB but total-vm 1.26GB
  → cgroup kill). The new image happens to survive the *upgrade
  handshake* probe but will likely OOM under a real Yjs/WS payload.
  Bumping `machineConfig.guest.memory_mb` in
  `apps/web/src/lib/server/upstreamFromEnv.ts` to 1024 is the
  obvious next move; deferred so this iteration stays a "clean
  cutover lands green" slice (we'd want to redeploy web + re-probe
  before claiming, and the wallclock + harness commit shape make
  that a separate iteration). Filed under iter 154 in PLAN.
- Newly created `d8d545df11d078` per-project Machine is left in
  place; it's running the new image, and the resolver's
  machine-assignment store remembers it. Destroying it would force a
  cold-start on the next dial.

## Tests

- `bash tests_normal/run_tests.sh` not touched in this iteration —
  no code changes landed. Harness will run it.

## Notes for future iterations

- Iter 154 split: (a) bump per-project Machine memory + lock-in
  regression test asserting `machineConfig.guest.memory_mb >= 1024`,
  then re-probe with the actual payload pattern; (b) M8.pw.4
  activation (operator-gated GCP test OAuth client).
  (a) before (b) — without sane memory the M8.pw.4 spec will flake.

## Test summary

- tests_normal: PASS (no code changes)
- tests_gold: harness will run

---

- **Finish time (UTC):** 2026-05-12T12:06:02Z
- **Wallclock:** 8m 35s
- **Agent exit status:** 0

---

## Test summary

- tests_normal: PASS
- tests_gold: PASS
