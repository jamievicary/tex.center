# Iteration 145

- **Start commit:** `c000f36`
- **Start time (UTC):** 2026-05-12T09:34:39Z

---

## Goal for this iteration

Discussion mode (`.autodev/discussion/145_question.md`). User
reports the live product at https://tex.center is broken: typing
in the editor doesn't save, create-file does nothing, PDF preview
never renders. Diagnosis hypothesis: WS path (browser →
`wsProxy.ts` → sidecar) is failing. Question asks to (a)
acknowledge the pivot, (b) pause cron rituals + M7.4.2 + post-MVP
M7 work, (c) diagnose WS live, (d) eventually rewrite PLAN.md.

See `.autodev/discussion/145_answer.md` for the full reply.

## Actions

- Wrote `145_answer.md` engaging with the diagnosis. Agreement on
  WS hypothesis + iter-76 pattern repeating + pause cron + PLAN
  rewrite. One refinement: GCP OAuth test-client provisioning is
  browser-only, so the "activate M8.pw.4 now" literal sequence
  needs operator help. Authed-WS direct probe (no OAuth involved)
  is a faster diagnostic path I can drive myself; proposed that
  for next iteration instead.

- **Anon WS probe of live**, `wss://tex.center/ws/project/00000000-
  ...-000000000000` with no cookie via raw `https.request` +
  Upgrade headers. Got HTTP/1.1 401, `connection: close`, empty
  body, `fly-request-id: 01KRDRNTYYN286TN23HSHT0H1Z-lhr`. This
  rules out catastrophic failure modes (Fly routing, control-plane
  crash on upgrade, missing WS handler). The "auth gate denies a
  real user too" and "upgrade succeeds but sidecar wedged"
  hypotheses both remain plausible.

- Updated `.autodev/PLAN.md`:
  - Added top-of-file "P0: live MVP non-functional" block with
    hypothesis, anon-probe result, next-iter plan, pause list.
  - Marked old "Current focus → M7.4.2" as superseded.

## Tests

- `bash tests_normal/run_tests.sh` — PASS (112 tests, ~45s).
- gold not touched this iteration.

## Notes for future iterations

- **Next iter:** authed WS probe of live. Single-purpose. Use
  `creds/fly-postgres.txt` to `flyctl proxy` live PG → connect
  drizzle → `mintSession` for the user in `creds/live-user-id.txt`
  → raw WS handshake against a real project ID owned by that user
  with the `tc_session` cookie. Capture status + body + any
  upgrade-then-hang behaviour. Don't fix anything yet — diagnosis
  only. The failure layer determines what gets fixed in iter 147.
- **Iter 147:** fix root cause + regression test.
- **Iter 148:** wire authed-WS check into deploy-time spec
  (`verifyLive.spec.ts` or similar) so future regressions of
  this class can't ship silently.
- **Iter 149 (or thereabouts):** PLAN.md rewrite per the spec in
  the question (≤80 lines, critical-path-first).
- Cron rituals (N%10 refactor, N%11 plan-review) stay paused
  until the deploy-time WS check is live. The P0 block at the
  top of PLAN.md is the source of truth on this.
- Operator step still useful (but not blocking): provision GCP
  test OAuth client per PLAN's "Live activation" section. With
  that, M8.pw.4 can become the deploy-gate instead of just an
  authed-WS probe.

---

- **Finish time (UTC):** 2026-05-12T09:38:38Z
- **Wallclock:** 3m 59s
- **Agent exit status:** 0

---

## Test summary

- tests_normal: PASS
- tests_gold: PASS
