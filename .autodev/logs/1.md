# Iteration 1

- **Start commit:** `d6ac70d`
- **Start time (UTC):** 2026-05-10T16:40:15Z

---

## Goal for this iteration

First iteration on a fresh checkout: `.autodev/PLAN.md` is empty, no
`src/` content, both test runners are placeholder no-ops. Per
engineer.md §3, the right move is to draft an initial plan from
`GOAL.md` rather than start coding. This unblocks subsequent
iterations to pick concrete milestones.

## Approach

Read `GOAL.md` and `vendor/supertex/GOAL.md` (since the project is
intimately tied to supertex's own state), then write a milestone plan
that splits the work into a vertical slice runnable locally (M0–M2),
supertex daemon-isation (M3), persistence (M4), auth (M5), Fly deploy
+ per-project Machines (M6–M7), and an acceptance pass (M8). Set
"Current focus" on M0 (monorepo scaffolding) so iter 2 has an
unambiguous starting point.

## Actions

- Wrote `.autodev/PLAN.md` (overview, 9 milestones, current focus on
  M0, open questions, candidate upstream supertex PRs).
- No source / test changes this iteration.

## Tests

`tests_normal/run_tests.sh` and `tests_gold/run_tests.sh` are
placeholder no-ops (PASS by design). Did not run them; nothing
exercised them and there is no risk of regression from a
plan-only iteration.

## Notes for future iterations

- Iter 2 should land M0: pnpm workspace with `apps/web`,
  `apps/sidecar`, `packages/protocol`, root `tsconfig`, and rewire
  `tests_normal/run_tests.sh` to run `pnpm -r typecheck` (plus any
  unit tests). Keep the slice small — one workspace skeleton, no
  feature code.
- The four candidate supertex upstream items in PLAN.md "Candidate
  supertex (upstream) work" should be cross-checked against
  `vendor/supertex/PLAN.md` before M3 starts; they may be partially
  done already.
- `creds/` already has all four token files — auth (M5) and deploy
  (M6) won't be blocked on missing credentials.

---

- **Finish time (UTC):** 2026-05-10T16:42:02Z
- **Wallclock:** 1m 47s
- **Agent exit status:** 0

---

## Test summary

- tests_normal: PASS
- tests_gold: PASS
