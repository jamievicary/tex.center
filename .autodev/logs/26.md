# Iteration 26

- **Start commit:** `0d38713`
- **Start time (UTC):** 2026-05-10T19:37:46Z

---


## Goal for this iteration

Small, coherent cleanup: iteration 25 accidentally committed a
stray editor temp file `apps/sidecar/src/server.ts.tmp.28786.1778441530387`
(byte-identical to `server.ts`, created by the Edit tool's
atomic-write path mid-iteration). It's tracked in git, which is
why `git status` is clean and why this slipped through. Remove
the file and add a `.gitignore` rule covering the
`*.tmp.<digits>.<digits>` pattern (and the bare `*.tmp` form used
by `ProjectWorkspace.writeMain`) so the next leak — should one
happen — stays untracked rather than landing in a commit.

No forward-progress milestone work bundled with this; cleanup is
its own iteration. Next ordinary iteration picks up the choices
already in `## Current focus`: multi-file project support,
auth scaffolding, or upstream supertex PRs.

## Approach

1. `git rm` the stray file.
2. Add `*.tmp` and `*.tmp.[0-9]*` to `.gitignore` under the
   editor/OS section.
3. Run normal tests; confirm green.

## Actions

- `git rm apps/sidecar/src/server.ts.tmp.28786.1778441530387`
  (was byte-identical to `server.ts`, added in commit `0d38713`
  / iteration 25 as an accidental staging of an Edit-tool temp
  file).
- `.gitignore` — added `*.tmp` and `*.tmp.[0-9]*` with an
  explanatory comment under the OS/editor section.

## Tests

`bash tests_normal/run_tests.sh` — PASS (17/17).

Gold suite not run: no gold-side changes; the PGlite gold case
is expensive and unrelated to this cleanup.

## Notes for future iterations

- If a similar leak ever recurs from a different filename pattern
  (e.g. an editor that writes `<name>~` backups), tighten the
  ignore list rather than papering over it — the leak itself is
  the bug, the ignore is a safety net.
- Forward focus remains where iteration 25 left it: multi-file
  project hydration, M5 auth scaffolding, or upstream supertex
  PRs for `--ready-marker` / `--target-page=N`.

---

- **Finish time (UTC):** 2026-05-10T19:40:58Z
- **Wallclock:** 3m 12s
- **Agent exit status:** 0

---

## Test summary

- tests_normal: PASS
- tests_gold: PASS
