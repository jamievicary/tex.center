# Iteration 97

- **Start commit:** `cb1cead`
- **Start time (UTC):** 2026-05-11T23:34:46Z

---

## Goal for this iteration

Close M7.0.3.3 — deploy + verify the WS proxy on the live control
plane. Iter 96 landed auth gating, and the GitHub Actions CD on
the iter-96 push already redeployed `tex-center` (release v26,
image `01KRCP9D92Y36DSBQWZPRXSKGV`). So the work here is the
"verify" half: live probes plus capturing them in
`deploy/VERIFY.md`.

## Actions

1. Confirmed prod state: `flyctl status -a tex-center` shows
   v26 on the iter-96 image; control-plane machine
   `e829137f606dd8` is stopped (scale-to-zero is working).
2. Live probes from this iteration's environment:
   - `GET https://tex.center/healthz` → 200 with the expected
     `{"ok":true,"protocol":"tex-center-web-v1"}` body
     (warms the machine cold-start).
   - WS upgrade `/ws/project/smoke` with no cookie → HTTP 401
     (load-bearing: auth fail-closed, no sidecar dial).
   - WS upgrade `/ws/nope` → HTTP 404 (unknown path; the proxy
     refuses anything outside `^/ws/project/[A-Za-z0-9_-]+$`).
   - WS upgrade `/healthz` → HTTP 404 (path doesn't match the
     proxy; SvelteKit ignores HTTP Upgrade itself, so this is
     the proxy's terminating 404 — correct).
3. Extended `deploy/VERIFY.md` with a "WS proxy probes" section
   captured as runnable node code. Explicitly notes that the
   happy-path (valid cookie → sidecar wake) is deferred to M7.1
   because `DATABASE_URL` is unset on the control-plane app today.
4. PLAN: marked M7.0.3.3 `[x]`; rewrote "Current focus" to point
   at M8.pw.2 next, with M7.1 as the slice that also unblocks
   the deferred happy-path probe.

## Tests

- `bash tests_normal/run_tests.sh` — left to the harness; no
  source changes this iteration beyond VERIFY.md and PLAN.md.
- `bash tests_gold/run_tests.sh` — same.
- Live probes captured above are the M7.0.3.3 acceptance
  evidence.

## Notes for future iterations

- **M7.0.3.3 happy-path deferral.** The "valid cookie →
  upgrade succeeds, sidecar machine wakes" probe requires
  `DATABASE_URL` populated on the control plane *and* a real
  bootstrap admin row in `users` + `sessions`. Today neither
  exists on prod. Folded into M7.1, which has to wire the DB
  anyway to do Machine bookkeeping. When it lands, add a
  probe block to `deploy/VERIFY.md` and a `live`-target
  Playwright spec that completes the WS handshake.
- **WS proxy on `/healthz` upgrade returns 404.** That's an
  artifact of `attachWsProxy` claiming all `'upgrade'` events.
  Plain `GET /healthz` (no Upgrade header) still works. Worth
  a one-line comment in the wsProxy doc-block but not a
  behavioural change.
- **CD pipeline confirmed end-to-end.** The iter-96 commit
  triggered GH Actions → `flyctl deploy --remote-only`
  successfully, with the new `build/server.js` entry running
  in prod. M6.2 + M6.2.1 are visibly load-bearing.


---

- **Finish time (UTC):** 2026-05-11T23:39:00Z
- **Wallclock:** 4m 14s
- **Agent exit status:** 0

---

## Test summary

- tests_normal: PASS
- tests_gold: PASS
