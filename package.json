{
  "name": "tex-center",
  "version": "0.0.0",
  "private": true,
  "description": "Cloud LaTeX editor on top of supertex (https://tex.center).",
  "license": "UNLICENSED",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20.10.0"
  },
  "scripts": {
    "typecheck": "pnpm -r --parallel typecheck",
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.3.16",
    "@electric-sql/pglite-socket": "^0.0.22",
    "@playwright/test": "1.49.1",
    "@tex-center/auth": "workspace:*",
    "@tex-center/db": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "tsx": "^4.19.1",
    "typescript": "5.6.3"
  }
}
