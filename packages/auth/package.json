{
  "name": "@tex-center/auth",
  "version": "0.0.0",
  "private": true,
  "description": "Pure-logic auth primitives for tex.center: HMAC-signed session tokens and the email allowlist policy. No I/O, no network — wired into apps/web (and tests) as a leaf module.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^20.16.13",
    "tsx": "^4.19.1"
  }
}
