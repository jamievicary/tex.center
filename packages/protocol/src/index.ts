// Wire protocol between apps/web and apps/sidecar.
//
// Filled in across M2+. Kept as a separate package from the start so
// browser and server cannot drift on message shapes.

export const PROTOCOL_VERSION = 0;
