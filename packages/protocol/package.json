{
  "name": "@tex-center/protocol",
  "version": "0.0.0",
  "private": true,
  "description": "Wire protocol shared between apps/web (browser) and apps/sidecar (per-project server): Yjs channels, compile messages, PDF byte-range patch messages.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
