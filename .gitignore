# Credentials — NEVER commit
creds/
*.token
*.pem
*.key
.env
.env.*
!.env.example

# Node toolchain (vendored per-checkout, see tests_normal/setup_node.sh)
.tools/

# Node
node_modules/
.pnpm-store/
dist/
build/
.svelte-kit/
.next/
.turbo/
.cache/
coverage/
*.log
npm-debug.log*
pnpm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Local TeX/build artefacts
*.aux
*.log
*.pdf
*.synctex.gz
*.fls
*.fdb_latexmk
*.out
*.toc
# Frontend static fixtures are intentionally tracked.
!apps/web/static/*.pdf
# Sidecar stub-compile fixture (will be deleted at M3 when supertex lands).
!apps/sidecar/fixtures/*.pdf

# Autodev internal state — keep out of the product repo
autodev/
/autodev/
